name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  macos:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - name: Install tools
        run: brew install just cmake

      - name: Show toolchain
        run: |
          rustc --version
          cargo --version
          swift --version
          xcodebuild -version

      - name: Verify
        run: just verify
